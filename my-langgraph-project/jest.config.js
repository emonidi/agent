// jest.config.js
module.exports = {
  // verbose: true, // Example: enable verbose output
  testEnvironment: 'node',
  // clearMocks: true, // Automatically clear mock calls and instances between every test
};
